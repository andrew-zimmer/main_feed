<%= f.label :user_id, 'User:' %><br>
<%= f.collection_select :user_id, User.all, :id, :full_name %><br><br>

<%= f.label :expiration, "Expiration:"%><br>
<%= f.date_field :expiration %><br><br>

<%= f.label :badge_id, 'Badge:' %><br>
<%= f.collection_select :badge_id, Badge.all, :id, :full_name, prompt: true %><br>
