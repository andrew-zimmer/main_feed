<h1> Foreman index </h1>
<ul>
<% foreman_all.each do |f| %>
    <li> <%=link_to "#{f.first_name} #{f.last_name}", foreman_path(f.foreman) %> </li>
    <%= link_to 'Edit', edit_foreman_path(f.foreman) %>
    <%= link_to 'View Helpers', foreman_helpers_path(f.foreman)%>
<% end %>
</ul>
