<h1> Foreman index </h1>
<ul>
<% @foremen.sort_by_name.each do |f| %>
    <li> <h3> <%=link_to "#{f.full_name}", foreman_path(f) %></h3> </li>
    <%= link_to 'Edit', edit_foreman_path(f) + '/' if can? :manage, Foreman %>
    <%= link_to 'View Helpers', foreman_helpers_path(f)%>
<% end %>
</ul>

<%= link_to "Back", :back %>
